import BottomNav from './index';

export default {
  title: 'Frame/BottomNav',
  components: BottomNav,
};

const Template = (args) => <BottomNav {...args} />;

export const Default = Template.bind({});